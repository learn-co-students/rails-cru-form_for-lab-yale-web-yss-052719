<h1>Editing an artist</h1>
<%= form_for(@artist) do |f| %>
<label>Name:</label><br>
<%= f.text_field :name, :value => "#{@artist.name}" %>
<label>Bio:</label><br>
<%= f.text_area :bio, :value => "#{@artist.bio}"%><br>
<%= f.submit %>
<% end %>