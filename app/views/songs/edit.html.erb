<h1>Editing an song</h1>
<%= form_for(@song) do |f| %>
<label>Name:</label><br>
<%= f.text_field :name, :value => "#{@song.name}" %>
<%= f.text_field :artist_id, :value => "#{@song.artist_id}"%>
<%= f.text_field :genre_id, :value => "#{@song.genre_id}" %>
<%= f.submit %>
<% end %>